name: result_or
description: "Easy error handling implementation based on ResultOrError/Either idea."
version: 0.8.8
homepage: https://github.com/aleexam/result_or
issue_tracker: https://github.com/aleexam/result_or/issues

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.3.0 <4.0.0'

dev_dependencies:
  test: ^1.20.0